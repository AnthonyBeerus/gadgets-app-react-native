/**
 * @deprecated Prefer `shared/design-system` motion tokens (`timingConfig`, `scale`, etc.).
 * Re-exports quiet-commerce motion for legacy imports.
 */
export {
  duration as DURATION,
  stagger as STAGGER,
  easing as EASING,
  timingConfig as TIMING_CONFIG,
  scrollThresholds as SCROLL_THRESHOLDS,
  scale as SCALE,
  opacity as OPACITY,
} from '../design-system/tokens/motion';
