import React from 'react'; import { StyleSheet, View } from 'react-native'; import { Text } from './Text'; import { useDesignTokens } from '../theme/DesignTokensProvider';
export function IdentityMark({id,name,person=false,size=44}:{id:string|number;name:string;person?:boolean;size?:number}){const t=useDesignTokens();const palette=[t.colors.creator,t.colors.payout,t.colors.commerce,t.colors.success];const backgroundColor=palette[[...String(id)].reduce((n,c)=>n+c.charCodeAt(0),0)%palette.length];const initials=name.trim().split(/\s+/).slice(0,2).map(x=>x[0]?.toUpperCase()).join('');return <View accessibilityLabel={name} style={[s.base,{width:size,height:size,borderRadius:person?size/2:0,borderColor:t.colors.stroke,backgroundColor}]}><Text variant="label" color={t.colors.onCreator}>{initials}</Text></View>}
export const Avatar=(p:Omit<React.ComponentProps<typeof IdentityMark>,'person'>)=><IdentityMark {...p} person/>; export const MerchantMark=(p:Omit<React.ComponentProps<typeof IdentityMark>,'person'>)=><IdentityMark {...p}/>; const s=StyleSheet.create({base:{borderWidth:2,alignItems:'center',justifyContent:'center'}});
