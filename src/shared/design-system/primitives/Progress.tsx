import React from 'react'; import { View } from 'react-native'; import { useDesignTokens } from '../theme/DesignTokensProvider';
export function Progress({ value }: { value: number }) { const t=useDesignTokens(); const pct=Math.max(0,Math.min(1,value))*100; return <View accessibilityRole="progressbar" accessibilityValue={{min:0,max:100,now:pct}} style={{height:14,borderWidth:2,borderColor:t.colors.stroke,backgroundColor:t.colors.surface}}><View style={{height:'100%',width:`${pct}%`,backgroundColor:t.colors.payout,borderRightWidth:pct>0&&pct<100?2:0,borderRightColor:t.colors.stroke}} /></View>; }
