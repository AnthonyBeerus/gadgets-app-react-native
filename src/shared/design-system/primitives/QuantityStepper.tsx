import React from 'react'; import { Pressable, StyleSheet, View } from 'react-native'; import { Text } from './Text'; import { useDesignTokens } from '../theme/DesignTokensProvider';
export function QuantityStepper({ value, onDecrement, onIncrement, minimum=1, maximum=99 }: {value:number;onDecrement:()=>void;onIncrement:()=>void;minimum?:number;maximum?:number}) { const t=useDesignTokens(); return <View style={[s.wrap,{borderColor:t.colors.stroke,backgroundColor:t.colors.surface}]}><Pressable accessibilityLabel="Decrease quantity" disabled={value<=minimum} onPress={onDecrement} style={s.key}><Text>−</Text></Pressable><View style={[s.count,{borderColor:t.colors.stroke}]}><Text variant="bodyBold" selectableData>{value}</Text></View><Pressable accessibilityLabel="Increase quantity" disabled={value>=maximum} onPress={onIncrement} style={s.key}><Text>+</Text></Pressable></View>; } const s=StyleSheet.create({wrap:{height:38,flexDirection:'row',borderWidth:2},key:{width:38,alignItems:'center',justifyContent:'center'},count:{width:32,alignItems:'center',justifyContent:'center',borderLeftWidth:2,borderRightWidth:2}});
