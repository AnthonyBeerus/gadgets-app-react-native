import React from 'react'; import { KeyboardAvoidingView, Platform, ScrollView, StyleSheet, View, type ScrollViewProps, type ViewProps } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { useRouter, type Href } from 'expo-router'; import { useDesignTokens } from '../theme/DesignTokensProvider'; import { ScreenHeader } from './ScreenHeader';
/** `fallbackHref` is the path's root — back never strands a user arriving with no history. */
export function StackScreenTemplate({title,fallbackHref,children,footer,scrollProps,step}: {title:string;fallbackHref:Href;children:React.ReactNode;footer?:React.ReactNode;scrollProps?:ScrollViewProps;step?:{current:number;total:number}}) { const t=useDesignTokens(); const router=useRouter(); const goBack=()=>router.canGoBack()?router.back():router.replace(fallbackHref); // The footer owns the bottom inset, so the frame must not add it a second time.
 return <SafeAreaView edges={footer?['top']:['top','bottom']} style={[s.safe,{backgroundColor:t.colors.canvas}]}><ScreenHeader title={title} step={step} onBack={goBack}/><KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={s.safe}><ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={s.content} {...scrollProps}>{children}</ScrollView>{footer}</KeyboardAvoidingView></SafeAreaView>; }
export function StatusScreenTemplate({children}:ViewProps){const t=useDesignTokens();return <SafeAreaView style={[s.safe,{backgroundColor:t.colors.canvas}]}><View style={s.status}>{children}</View></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1},content:{padding:18,paddingBottom:32,gap:18},status:{flex:1,justifyContent:'center',padding:18,gap:16}});
