import React from 'react'; import { KeyboardAvoidingView, ScrollView, StyleSheet, View, type ScrollViewProps, type ViewProps } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { useRouter, type Href } from 'expo-router'; import { useDesignTokens } from '../theme/DesignTokensProvider'; import { ScreenHeader } from './ScreenHeader';
export function StackScreenTemplate({title,fallbackHref,children,footer,scrollProps}: {title:string;fallbackHref:Href;children:React.ReactNode;footer?:React.ReactNode;scrollProps?:ScrollViewProps}) { const t=useDesignTokens(); const router=useRouter(); const goBack=()=>router.canGoBack()?router.back():router.replace(fallbackHref); return <SafeAreaView edges={['top','bottom']} style={[s.safe,{backgroundColor:t.colors.canvas}]}><ScreenHeader title={title} onBack={goBack}/><KeyboardAvoidingView behavior="padding" style={s.safe}><ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={s.content} {...scrollProps}>{children}</ScrollView>{footer}</KeyboardAvoidingView></SafeAreaView>; }
export function StatusScreenTemplate({children}:ViewProps){const t=useDesignTokens();return <SafeAreaView style={[s.safe,{backgroundColor:t.colors.canvas}]}><View style={s.status}>{children}</View></SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1},content:{padding:18,paddingBottom:32,gap:18},status:{flex:1,justifyContent:'center',padding:18,gap:16}});
